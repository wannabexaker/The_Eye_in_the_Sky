node_modules
.next
dist
.turbo
coverage

# Local working assets only. Never touch, never commit.
/NOT-FOR-USE/

# TypeScript incremental/build artifacts
*.tsbuildinfo

# Local environment files (never commit secrets or local endpoints)
.env
.env.*
!.env.example
!.env*.example
graphify_run.py

# Local credentials reference (git-ignored, development only)
CREDENTIALS-LOCAL-REFERENCE.md

# Local API runtime helper output
apps/api/.runtime-api*.log

# Local editor-only settings
.vscode/settings.json

# Dev tooling - not needed for app to function
.claude/
.codex/
CLAUDE.md
graphify-out/

# Local visual/test artifacts
apps/player-web/screenshots/
apps/player-web/test-results/
.audit-shots/
apps/player-web/e2e/_*.spec.ts

# Local editor workspaces
.obsidian/
.idea/

# Local analysis cache (skill output - never commit)
docs/notes.md
