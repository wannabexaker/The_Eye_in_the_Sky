node_modules
.next
dist
.turbo
coverage

# Working assets (local development only, never commit)
/assets/

# TypeScript incremental/build artifacts
*.tsbuildinfo

# Local environment files (never commit secrets or local endpoints)
.env
.env.*
!.env.example
!.env*.example

# Local editor-only settings
.vscode/settings.json
