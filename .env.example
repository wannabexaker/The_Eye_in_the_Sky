# PostgreSQL container settings for docker-compose.yml.
# Replace placeholders locally. Never commit real passwords.
POSTGRES_PASSWORD="__SET_POSTGRES_PASSWORD__"
POSTGRES_PORT="5432"

# Optional public/proxy origins.
CORS_ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3100,http://localhost:3200"
